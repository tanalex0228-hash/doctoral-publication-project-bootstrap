FROM python:3.11-slim
ENV PYTHONDONTWRITEBYTECODE=1 PYTHONUNBUFFERED=1
WORKDIR /app
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt
COPY . .
RUN useradd --create-home appuser \
    && mkdir -p /var/lib/doctoral-private-media /var/lib/doctoral-static \
    && chown -R appuser:appuser /app /var/lib/doctoral-private-media /var/lib/doctoral-static
USER appuser
CMD ["gunicorn", "config.wsgi:application", "--bind", "0.0.0.0:8000", "--workers", "3", "--access-logfile", "-", "--error-logfile", "-", "--capture-output", "--timeout", "60"]
